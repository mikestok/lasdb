$("div.question#<%= @question.id %>").html("<%=
 j render(:partial => 'question',
          :locals => {
            :id      => @question.id,
            :prompt  => @question.prompt,
            :answers => @question.answer_list(:shuffled => true)
          }
         )
%>");
